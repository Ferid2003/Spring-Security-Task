<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%X{transactionId}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Application code at DEBUG level -->
    <logger name="farid.aghazada" level="DEBUG"/>

    <!-- Suppress noisy Hibernate/Spring internals -->
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.orm" level="WARN"/>
    <logger name="org.springframework.web" level="WARN"/>
    <logger name="org.springframework.data" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
